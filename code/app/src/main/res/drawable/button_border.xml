<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- This is the stroke you want to define -->
    <stroke android:width="0.5dp"
        android:color="@color/colorBlack"/>

    <!-- Optional, round your corners -->
    <corners android:bottomLeftRadius="1dp"
        android:topLeftRadius="1dp"
        android:bottomRightRadius="1dp"
        android:topRightRadius="1dp" />

    <!-- Optional, fill the rest of your background with a color or gradient, use transparent if you only want the border to be displayed-->
    <gradient android:startColor="@color/grey_trans"
        android:endColor="@color/grey_trans"
        android:angle="45" />

</shape>